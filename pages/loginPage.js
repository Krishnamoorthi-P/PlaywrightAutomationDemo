
import { expect }  from '@playwright/test';
export class loginPage {
  constructor(page) {
    //access the page here
    this.page = page;

    this.userName = page.l;
    this.password = password;
  }

  async login(){
    
    
  }
}
